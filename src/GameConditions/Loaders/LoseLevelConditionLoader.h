#pragma once

#include "Conditions/Loaders/ConditionsLoader.h"

namespace asteroids
{
    /*class LoseLevelCondition;

    class LoseLevelConditionLoader
        : public shen::ConditionLoader
    {
        CLASS_LOADER(shen::ConditionLoadersCollection, LoseLevelCondition)

    public:
        std::unique_ptr<shen::ConditionBase> LoadCondition(const shen::DataElementWrapper& elementWrapper) override;
    };*/
}
