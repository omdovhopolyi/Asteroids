#include "FireCommandLoader.h"
#include "GameCommands/FireCommand.h"

namespace asteroids
{
    /*REGISTER_CLASS_LOADER(FireCommandLoader)

    std::unique_ptr<shen::Command> FireCommandLoader::LoadCommand(const shen::DataElementWrapper& elementWrapper)
    {
        auto command = std::make_unique<FireCommand>();
        LoadTypeAndId(elementWrapper, command.get());
        return command;
    }*/
}
